version: "3.7"

x-settings:
  projectName: payslip # Nome explícito do projeto
  
services:
  payslip-api:
    image: vitornikiforck/payslip:latest
    build:
      context: ./
      dockerfile: ./src/Payslip.Api/Dockerfile
    container_name: payslip-api
    ports:
      - 80:80
      - 443:443
